/**
 * PostCSS configuration for Tailwind CSS v4.
 * v4 ships its PostCSS integration as a dedicated plugin package.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
